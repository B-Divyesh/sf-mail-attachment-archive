[
  {"id":"demo-sandbox","claim":"Try the product in one click with sample data that never enters real storage.","where":"landing first screen, /demo, README","test":"npm run test:e2e -- --grep @claim:demo-sandbox","sandbox":"fresh browser context; enter from landing; inspect storage; search; reset; leave demo"},
  {"id":"local-only","claim":"Mail processing stays on the user's device and the demo sends no data off origin.","where":"landing facts and privacy section, /privacy, README","test":"npm run test:e2e -- --grep @claim:local-only","sandbox":"fresh browser context; intercept every request during the sample search and filter flow"},
  {"id":"mbox-import","claim":"Imports a standard MBOX mail export.","where":"landing how-it-works, desktop empty state, README","test":"cargo test --manifest-path src-tauri/Cargo.toml claim_mbox_import","sandbox":"bundled standards-shaped message; parse headers and MBOX boundary without network"},
  {"id":"sha256-dedup","claim":"Hashes attachment bytes with SHA-256 and stores identical files once while retaining both references.","where":"landing how-it-works, README","test":"cargo test --manifest-path src-tauri/Cargo.toml claim_sha256_dedup","sandbox":"temporary MBOX with two references to the same bytes; assert two records and one stored payload"},
  {"id":"encrypted-integrity","claim":"A passphrase-gated reopen scan authenticates encrypted files, never stores the passphrase, and reports damaged ciphertext as corrupt.","where":"desktop verification panel, privacy page, README","test":"cargo test --manifest-path src-tauri/Cargo.toml claim_encrypted_integrity","sandbox":"temporary encrypted archive; inspect manifest; reopen; scan; mutate ciphertext; reject wrong passphrase; rescan and assert report"},
  {"id":"restore-integrity","claim":"Attachments stay inert in hash-named storage, and restoration verifies SHA-256 before writing the destination.","where":"README and desktop restore result","test":"cargo test --manifest-path src-tauri/Cargo.toml claim_restore_integrity","sandbox":"temporary archive; assert inert path; restore known bytes; damage stored object; assert next destination is not written"},
  {"id":"evidence-reports","claim":"Exports CSV and JSON evidence reports with attachment records and failures.","where":"landing how-it-works, README","test":"cargo test --manifest-path src-tauri/Cargo.toml claim_evidence_reports","sandbox":"temporary imported archive; export both formats and parse the files"},
  {"id":"sample-evidence","claim":"The sample resolves three of four references, shows one duplicate, and keeps one decode failure visible.","where":"landing preview, /demo","test":"npm run test:e2e -- --grep @claim:sample-evidence","sandbox":"fresh /demo state; assert exact counts and visible status evidence"},
  {"id":"csv-report","claim":"The demo exports its complete verification report as CSV.","where":"/demo Export CSV action","test":"npm run test:e2e -- --grep @claim:csv-report","sandbox":"fresh /demo state; download CSV and assert header, four reference rows, and one issue row"},
  {"id":"archive-search","claim":"Searches attachment filenames, senders, subjects, and checksums and filters by status.","where":"desktop archive toolbar, README","test":"npm run test:e2e -- --grep @claim:archive-search","sandbox":"fresh /demo state; search by sender and filter to duplicate status"},
  {"id":"plus-price","claim":"Archive Plus costs US $29 once and uses the Sociobot checkout.","where":"landing pricing, /terms, README","test":"npm run test:e2e -- --grep @claim:plus-price","sandbox":"fresh landing page; assert price and checkout URL without navigating"},
  {"id":"free-core","claim":"Import, open, sample, search, and report export remain available without a license.","where":"landing pricing, desktop app, README","test":"npm run test:e2e -- --grep @claim:free-core","sandbox":"fresh app shell with no local storage or license; assert each core entry point and sample export"},
  {"id":"license-daily","claim":"Only a license token is sent for verification, at most once per day, and the token is stripped from the return URL.","where":"privacy page and Archive Plus restore flow","test":"npm test -- --testNamePattern @claim:license-daily","sandbox":"memory-only storage and mocked billing response; capture URL token and call verification twice"},
  {"id":"release-assets","claim":"The release manifest lists macOS ARM and Intel, Windows, Linux AppImage, and Linux DEB downloads with SHA-256 values.","where":"download control, README","test":"npm test -- --testNamePattern @claim:release-assets","sandbox":"parse the shipped same-origin manifest and validate platform URLs and checksums"}
]
